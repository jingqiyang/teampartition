Script to automate balanced co-ed volleyball team creation.

Default workbook is `test_data.xlsx`. Reads from sheet labeled with the current year.

Usage:
```
python3 partition.py <input_workbook>
```
